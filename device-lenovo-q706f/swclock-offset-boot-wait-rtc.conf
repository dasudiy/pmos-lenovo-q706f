[Unit]
# Clear any wait on /sys/subsystem/rtc/devices/rtc0 — that path does not
# exist on this kernel (rtc0 lives under /sys/class/rtc/rtc0). Waiting for
# the missing systemd device unit delays boot by ~90s every time.
# rtc0 is registered early; late probe is covered by 90-swclock-offset-rtc.rules.
After=
Wants=
